<form method="post" action="{{ path('app_wp_site_delete', {'id': wp_site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wp_site.id) }}">
    <button class="btn">Delete</button>
</form>
